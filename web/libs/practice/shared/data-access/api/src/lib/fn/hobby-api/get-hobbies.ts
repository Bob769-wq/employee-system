/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Hobby } from '../../models/hobby';
import { PaginatedCollection } from '../../models/paginated-collection';

export interface GetHobbies$Params {

/**
 * The page number
 */
  page?: number;

/**
 * The page size
 */
  pageSize?: number;

/**
 * The field to order by
 */
  orderBy?: string;

/**
 * Text search
 */
  searchText?: string;
}

export function getHobbies(http: HttpClient, rootUrl: string, params?: GetHobbies$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedCollection & {
'items': Array<Hobby>;
}>> {
  const rb = new RequestBuilder(rootUrl, getHobbies.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('orderBy', params.orderBy, {});
    rb.query('searchText', params.searchText, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedCollection & {
      'items': Array<Hobby>;
      }>;
    })
  );
}

getHobbies.PATH = '/hobbies';
